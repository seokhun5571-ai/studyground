# 🔐 토큰 권한 문제 해결

## ❌ 문제

`403 Permission denied` 오류가 발생했습니다.

이는 토큰에 저장소 접근 권한이 없거나, 권한이 부족하다는 의미입니다.

## ✅ 해결 방법

### Fine-grained Token (github_pat_로 시작)인 경우

현재 토큰이 `github_pat_`로 시작하는 fine-grained token입니다.

이 토큰은 저장소별로 권한을 설정해야 합니다:

1. **토큰 설정 확인**
   - https://github.com/settings/tokens 접속
   - 생성한 토큰 클릭
   - "Repository access" 확인
   - `studyground` 저장소가 선택되어 있는지 확인

2. **저장소 접근 권한 추가**
   - 토큰 설정에서 "Repository access" → "Selected repositories"
   - `studyground` 저장소 선택
   - 저장

3. **권한 확인**
   - "Repository permissions" 섹션에서:
     - ✅ Contents: Read and write
     - ✅ Metadata: Read-only (자동)
     - ✅ Pull requests: Read and write (선택사항)

### Classic Token (ghp_로 시작) 사용 권장

Fine-grained token 대신 Classic token을 사용하는 것이 더 간단합니다:

1. **새 Classic Token 생성**
   - https://github.com/settings/tokens 접속
   - "Generate new token (classic)" 클릭
   - Note: `studyground-deploy`
   - Expiration: `90 days`
   - **Scopes**: `repo` 체크 (전체 repo 권한)
   - "Generate token" 클릭
   - 토큰 복사 (`ghp_`로 시작)

2. **푸시**
   ```bash
   cd /Users/ronnie/studyground-mvp
   ./안전한_푸시.sh
   ```
   새 토큰 입력

## 🔄 대안: 저장소 권한 확인

저장소가 Private인 경우:
- 토큰에 해당 저장소에 대한 접근 권한이 있어야 합니다
- Fine-grained token: 저장소를 명시적으로 선택해야 함
- Classic token: 모든 저장소에 접근 가능

## 💡 권장 방법

**Classic Token 사용 (가장 간단):**

1. https://github.com/settings/tokens
2. "Generate new token (classic)"
3. `repo` 권한 선택
4. 토큰 생성 및 복사
5. `./안전한_푸시.sh` 실행

---

**새 Classic Token을 생성하시면 알려주세요. 제가 푸시해드리겠습니다!**

