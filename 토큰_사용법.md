# 🔐 Personal Access Token 사용법

## 문제 해결

GitHub에서 비밀번호 인증이 더 이상 지원되지 않습니다. Personal Access Token을 사용해야 합니다.

## ✅ 해결 방법

### 1단계: Personal Access Token 생성 (1분)

1. **토큰 생성 페이지 접속**
   - https://github.com/settings/tokens 접속
   - 또는 GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)

2. **새 토큰 생성**
   - "Generate new token (classic)" 클릭
   - Note: `studyground-deploy` (아무 이름)
   - Expiration: `90 days` (또는 원하는 기간)
   - **권한 선택**: `repo` 체크 (전체 repo 권한)
   - "Generate token" 클릭

3. **토큰 복사**
   - 생성된 토큰을 **즉시 복사** (한 번만 보여줌!)
   - 예: `ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`

### 2단계: 푸시하기

**방법 1: 자동 스크립트 사용 (추천)**
```bash
cd /Users/ronnie/studyground-mvp
./푸시하기.sh
```
토큰을 물어보면 붙여넣기

**방법 2: 수동 푸시**
```bash
cd /Users/ronnie/studyground-mvp

# URL에 토큰 포함
git remote set-url origin https://seokhun5571-ai:YOUR_TOKEN@github.com/seokhun5571-ai/studyground.git

# 푸시
git push -u origin main

# 보안을 위해 URL에서 토큰 제거
git remote set-url origin https://github.com/seokhun5571-ai/studyground.git
```

**방법 3: Git Credential Helper 사용**
```bash
# 토큰을 credential helper에 저장
git config --global credential.helper osxkeychain

# 푸시 시도
git push -u origin main
# Username: seokhun5571-ai
# Password: (토큰 붙여넣기)
```

---

## ⚠️ 주의사항

1. **토큰은 비밀번호처럼 취급하세요**
   - 공유하지 마세요
   - GitHub에 커밋하지 마세요

2. **토큰 만료**
   - 설정한 기간 후 만료됩니다
   - 만료되면 새 토큰 생성 필요

3. **토큰 권한**
   - 최소한의 권한만 부여하세요
   - `repo` 권한이면 충분합니다

---

## 🚀 푸시 완료 후

푸시가 성공하면:
1. https://github.com/seokhun5571-ai/studyground 에서 코드 확인
2. Railway 배포 진행
3. 완료! 🎉

