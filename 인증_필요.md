# 🔐 GitHub 인증 필요

## 현재 상황

원격 저장소는 설정되었지만, 푸시하려면 인증이 필요합니다.

저장소: https://github.com/seokhun5571-ai/studyground

## 해결 방법 (둘 중 하나 선택)

### 방법 1: Personal Access Token 사용 (추천)

1. **토큰 생성**
   - https://github.com/settings/tokens 접속
   - "Generate new token (classic)" 클릭
   - Note: `studyground-deploy`
   - Expiration: `90 days` (또는 원하는 기간)
   - 권한 선택: `repo` (전체 체크)
   - "Generate token" 클릭
   - **토큰 복사** (한 번만 보여줌!)

2. **터미널에서 푸시**
   ```bash
   cd /Users/ronnie/studyground-mvp
   git push -u origin main
   ```
   - Username: `seokhun5571-ai`
   - Password: **토큰 붙여넣기** (비밀번호 아님!)

### 방법 2: SSH 키 사용

1. **SSH 키 확인**
   ```bash
   ls -la ~/.ssh/id_*.pub
   ```

2. **SSH 키가 없다면 생성**
   ```bash
   ssh-keygen -t ed25519 -C "seokhyeon5571@gmail.com"
   ```

3. **SSH 키를 GitHub에 추가**
   - `cat ~/.ssh/id_ed25519.pub` 실행하여 키 복사
   - https://github.com/settings/keys 접속
   - "New SSH key" 클릭
   - 키 붙여넣기 및 저장

4. **SSH URL로 변경 (이미 완료됨)**
   ```bash
   git remote set-url origin git@github.com:seokhun5571-ai/studyground.git
   git push -u origin main
   ```

---

## 빠른 해결

**가장 쉬운 방법:**

1. https://github.com/settings/tokens 접속
2. "Generate new token (classic)" 클릭
3. `repo` 권한 선택
4. 토큰 생성 및 복사
5. 터미널에서:
   ```bash
   cd /Users/ronnie/studyground-mvp
   git push -u origin main
   ```
6. Username: `seokhun5571-ai`
7. Password: **토큰 붙여넣기**

---

**토큰을 생성하시면 알려주세요. 제가 푸시해드리겠습니다!**

