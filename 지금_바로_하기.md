# 🚀 지금 바로 하기!

## ✅ 1단계: GitHub 저장소 만들기 (30초)

1. **새 저장소 만들기** 클릭
   - 또는 직접: https://github.com/new 접속

2. **저장소 정보 입력**
   - Repository name: `studyground-mvp`
   - Description: (선택사항) "스터디그라운드 MVP 시스템"
   - Public 또는 Private 선택
   - ⚠️ **"Add a README file" 체크 해제** (이미 코드가 있으므로)
   - ⚠️ **"Add .gitignore" 체크 해제**
   - ⚠️ **"Choose a license" 선택 안 함**

3. **"Create repository" 클릭**

4. **저장소 URL 복사**
   - 페이지에 표시된 URL 복사
   - 예: `https://github.com/yourusername/studyground-mvp.git`

---

## ✅ 2단계: 터미널에서 푸시하기 (1분)

터미널을 열고 아래 명령어를 **순서대로** 실행하세요:

```bash
# 1. 프로젝트 폴더로 이동
cd /Users/ronnie/studyground-mvp

# 2. 원격 저장소 추가 (YOUR_USERNAME을 실제 사용자명으로 변경!)
git remote add origin https://github.com/YOUR_USERNAME/studyground-mvp.git

# 3. 브랜치 이름 확인
git branch -M main

# 4. 푸시!
git push -u origin main
```

**또는 자동 스크립트 사용:**
```bash
cd /Users/ronnie/studyground-mvp
./push-to-github.sh
```
저장소 URL을 물어보면 위에서 복사한 URL 붙여넣기

---

## ✅ 3단계: Railway 배포하기 (2분)

### Railway 가입 및 배포

1. **Railway 접속**: https://railway.app

2. **가입**
   - "Start a New Project" 클릭
   - "Login with GitHub" 클릭
   - GitHub 계정으로 로그인

3. **프로젝트 생성**
   - "Deploy from GitHub repo" 클릭
   - 방금 만든 `studyground-mvp` 저장소 선택
   - "Deploy Now" 클릭

4. **환경 변수 설정** (중요!)
   - 프로젝트 → "Variables" 탭 클릭
   - "New Variable" 클릭하여 4개 추가:
   
     | Name | Value |
     |------|-------|
     | `NODE_ENV` | `production` |
     | `PORT` | `5001` |
     | `JWT_SECRET` | (아래 명령어로 생성) |
     | `HOST` | `0.0.0.0` |
   
   - JWT_SECRET 생성 (터미널에서):
     ```bash
     openssl rand -hex 32
     ```
     생성된 값을 복사해서 Railway에 입력

5. **완료!** 🎉
   - Railway가 자동으로 배포 시작
   - 2-3분 후 배포 완료
   - "Settings" → "Domains"에서 URL 확인

---

## 📱 접속하기

배포 완료 후 Railway에서 제공하는 URL로 접속:

- **태블릿 키오스크**: `https://your-url.railway.app/kiosk`
- **관리자 대시보드**: `https://your-url.railway.app/admin`
- 로그인: `admin` / `admin1234`

---

## 📱 아이패드/휴대폰에 앱 설치

1. Safari/Chrome으로 배포된 URL 접속
2. 공유 버튼(⬆️) → "홈 화면에 추가"
3. 완료! 홈 화면에 앱 아이콘 생성 📱

---

## 🆘 문제 해결

### 푸시할 때 인증 오류가 나면

**Personal Access Token 사용:**

1. https://github.com/settings/tokens 접속
2. "Generate new token (classic)" 클릭
3. Note: `studyground-deploy`
4. Expiration: `90 days` (또는 원하는 기간)
5. 권한 선택: `repo` (전체 체크)
6. "Generate token" 클릭
7. 토큰 복사 (한 번만 보여줌!)
8. 푸시할 때 비밀번호 대신 토큰 입력

### "remote origin already exists" 오류

```bash
git remote remove origin
git remote add origin https://github.com/YOUR_USERNAME/studyground-mvp.git
git push -u origin main
```

---

**지금 바로 1단계부터 시작하세요!** 🚀

